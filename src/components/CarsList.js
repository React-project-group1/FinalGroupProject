/*import React from 'react';
import { Car } from './Car';

export const CarsList = ({cars, setCarDetails, form, setForm }) => {
	return <div className='car-list'>
		{
			cars.map((car, idx) => {
				return <Car car={car} key={idx} setCarDetails={setCarDetails} form={form} setForm={setForm} />
			})
		}
	</div>
} */
